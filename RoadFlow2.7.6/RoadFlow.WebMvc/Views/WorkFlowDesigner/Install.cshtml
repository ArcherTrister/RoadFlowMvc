@{
    Layout = null;
    string json = Request.Form["json"];
    string msg = new RoadFlow.Platform.WorkFlow().InstallFlow(json);
    RoadFlow.Platform.Log.Add("安装了流程", json + "(" + msg + ")", RoadFlow.Platform.Log.Types.流程相关);
    Response.Write(msg);
}
